# BWARS SECURITY TERMINAL

> Security doctrine, penal enforcement, and warfare readiness.

```dataview
TABLE classification, status
FROM ""
WHERE contains(authority, "BWARS")
SORT classification DESC
```